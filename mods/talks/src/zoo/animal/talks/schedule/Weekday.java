package zoo.animal.talks.schedule;

import zoo.animal.feeding.Task;

public class Weekday {
    private Task task; // to see if feeding module is accessible through transitive dependency of care module on feeding module
}
